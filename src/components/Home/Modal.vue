/* eslint-disable vue/no-mutating-props */
<template>
    <div class="modal" role="dialog">
        <div class="modal-dialog modal-dialog-centered" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="exampleModalLabel">{{data.id ? 'Update': 'Add'}} Product</h5>
                    <button type="button" class="close btn" @click="$emit('close-modal')" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <form>
                        <div class="form-group">
                            <label for="name">Product Name</label>
                            <input type="text" :value="data.name" class="form-control" id="name" placeholder="Product name">
                        </div>
                        <div class="form-group">
                            <label for="price">Price</label>
                            <input type="text" :value="data.price" class="form-control" id="price" placeholder="Price">
                        </div>
                        <div class="form-group">
                            <label for="category">Category</label>
                            <select id="sort" class="form-control" @change="setCategory">
                                <option selected>Choose Category</option>
                                <option value="1">Coffe</option>
                                <option value="2">Dessert</option>
                                <option value="3">Main Dish</option>
                                <option value="4">Juice</option>
                            </select>
                        </div>
                        <div class="form-group">
                            <label for="image">Image</label>
                            <input type="file" id="image" name="image" @change="handleFile" placeholder="Another input">
                        </div>
                    </form>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" @click="$emit('close-modal')">Close</button>
                    <button type="button" class="btn btn-primary" @click="$emit('fire-event')">{{data.id ? 'Update': 'Add'}}</button>
                </div>
            </div>
        </div>
    </div>
</template>

<script>
export default {
  // eslint-disable-next-line vue/multi-word-component-names
  name: 'Modal',
  props: {
    data: {
      type: Object
    },
    closeModal: {
      type: Function
    }
  },
  methods: {
    handleFile (e) {
      // eslint-disable-next-line vue/no-mutating-props
      this.data.image = e.target.files[0]
    },
    setCategory (event) {
      // eslint-disable-next-line vue/no-mutating-props
      this.data.idCategory = event.target.value
    }
  }
}
</script>

<style scoped>
.modal {
    display: block;
    border-bottom: 1px solid rgb(0, 0, 0, 0.25);
    box-shadow: 0 4px 3px rgb(0, 0, 0, 0.25);
}
</style>
